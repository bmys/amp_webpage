header#header-xl
  amp-mega-menu#menu(height='46' layout='fixed-height')
    nav(style='display: flex;flex-direction: row; height: 100%; align-items: center;')
      ul#hide-xs
        li
          span.flex-center.font-logo
            amp-img(alt='A view of the sea' src='static/svg/chess-knight-solid.svg' width='40' height='40')
            | Knight
        li.menu-item.font-roboto
          span(role='button') O nas
          .content-menu(role='dialog')
            ul
              li(on="tap:aboutus.scrollTo(duration=200)")  Informacje
              li(on="tap:members.scrollTo(duration=200)")
                | Członkowie
              li(on="tap:kontakt.scrollTo(duration=200)") Kontakt
        li.menu-item.font-roboto
          a(href='join.html') Do&lstrok;&aogon;cz do nas
          
header#header-xs
  span.flex-center.font-logo
    amp-img(alt='A view of the sea' src='static/svg/chess-knight-solid.svg' width='40' height='40')
    | Knight
  button#list.sidebar-open-btn.hamburger.hamburger--elastic(on='tap:sidebar.toggle')
    span.hamburger-box
      span.hamburger-inner
